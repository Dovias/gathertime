export type User = {
  id: number;
  email: string;
  password: string;
  firstName: string;
  lastName: string;
};

export type UserFullName = {
  id: number;
  firstName: string;
  lastName: string;
}

export type UserProfileInfo = {
  id: number;
  firstName: string;
  lastName: string;
}